# Optional — all of these already have working localhost defaults baked
# into the api/*.js files. Only create a real .env here if you need to point
# the frontend at non-default ports or a deployed backend.
VITE_IDENTITY_API_URL=http://localhost:8001/api/auth
VITE_STUDENT_API_URL=http://localhost:8000/api
VITE_BILLING_API_URL=http://localhost:8002/api
VITE_ENROLLMENT_API_URL=http://localhost:8003/api
VITE_AUDIT_API_URL=
